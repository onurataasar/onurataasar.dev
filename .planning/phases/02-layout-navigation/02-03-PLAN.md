---
phase: 02-layout-navigation
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Footer.tsx
  - src/components/CompactFooter.tsx
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: false
requirements: [LAY-06, LAY-01, LAY-02, LAY-05]

must_haves:
  truths:
    - "Homepage renders as a sequence of full-viewport color-block sections"
    - "Scrolling the homepage feels like walking through a gallery of posters"
    - "Footer on homepage is a full-width contact section with Say Hello CTA and social links"
    - "Non-homepage pages get a compact footer with social icons and copyright"
    - "Site builds and all pages render without errors"
  artifacts:
    - path: "src/components/Footer.tsx"
      provides: "Full homepage contact footer with Say Hello CTA"
      min_lines: 40
    - path: "src/components/CompactFooter.tsx"
      provides: "Minimal footer for non-homepage pages"
      exports: ["default"]
    - path: "src/app/page.tsx"
      provides: "Homepage with Section components for each content block"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/Section.tsx"
      via: "Section component import for each homepage block"
      pattern: "import.*Section"
    - from: "src/app/page.tsx"
      to: "src/components/Footer.tsx"
      via: "Homepage footer as last section"
      pattern: "import.*Footer"
    - from: "src/app/layout.tsx"
      to: "src/components/CompactFooter.tsx"
      via: "CompactFooter in root layout for all pages"
      pattern: "import.*CompactFooter"
---

<objective>
Create footer variants (full homepage contact section + compact for other pages) and wire the homepage to use Section components with color-block backgrounds.

Purpose: Completes the poster-gallery layout by structuring the homepage into distinct full-viewport color-block sections and providing appropriate footer treatment per page type.
Output: Footer.tsx (homepage contact section), CompactFooter.tsx (other pages), page.tsx restructured with Sections
</objective>

<execution_context>
@C:/Users/onura/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/onura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-navigation/02-CONTEXT.md
@.planning/phases/02-layout-navigation/02-RESEARCH.md
@.planning/phases/02-layout-navigation/02-01-SUMMARY.md

<interfaces>
<!-- Section component created in Plan 01 -->
From src/components/Section.tsx (created in Plan 01):
```tsx
interface SectionProps {
  id: string;
  colorToken: string; // "hero" | "about" | "skills" | "projects" | "contact"
  children: React.ReactNode;
  className?: string;
  fullHeight?: boolean; // defaults to true
  grid?: boolean; // adds 12-col grid to inner container
}
export function Section(props: SectionProps): JSX.Element;
```

Section color tokens available (from globals.css, added in Plan 01):
```css
--color-section-hero, --color-section-hero-text
--color-section-about, --color-section-about-text
--color-section-skills, --color-section-skills-text
--color-section-projects, --color-section-projects-text
--color-section-contact, --color-section-contact-text
```

Current homepage (page.tsx) — has hero, profile, skills sections with hardcoded violet/zinc:
```tsx
"use client";
export default function Home() { ... } // FadeIn, StaggerContainer, socials, skillCategories
```

Current Footer.tsx — uses hardcoded violet-500 and zinc colors:
```tsx
"use client";
const socials = [/* Medium, GitHub, LinkedIn */];
export default function Footer() { ... }
```

Available icons (react-icons, already installed):
```tsx
import { FaGithub, FaLinkedin, FaMedium } from "react-icons/fa";
import { HiOutlineMail } from "react-icons/hi";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create footer variants — homepage full contact + compact</name>
  <files>src/components/Footer.tsx, src/components/CompactFooter.tsx, src/app/layout.tsx</files>
  <action>
**1. Rewrite Footer.tsx as the homepage full contact section**

This component is the "Say Hello" contact section that appears as the LAST full-viewport color block on the homepage. It uses the "contact" color token (yellow background, charcoal text).

Structure:
- Wrap in a `<Section id="contact" colorToken="contact">` from Section.tsx
- Split layout using the 12-col grid: left side (col-span-12 lg:col-span-7) has the CTA, right side (col-span-12 lg:col-span-5) has contact info
- Left side:
  - "Say hello" in oversized Syne: `font-[family-name:var(--font-display)] text-[var(--font-size-h1)] font-bold`
  - A brief one-liner: "Got a project? Want to collaborate? Just want to say hi?"
  - A prominent mailto link styled as a large text link (not a button): `onurataasar@gmail.com` in `text-2xl lg:text-3xl font-semibold` with underline on hover
- Right side:
  - Social links in a vertical or horizontal list: GitHub, LinkedIn, Medium, Email
  - Each with icon + label text, using design tokens for colors
  - `text-lg` size, good spacing between items
- Bottom: copyright line `© {new Date().getFullYear()} Onur Ata Asar` in small muted text

Keep as "use client" for framer-motion hover effects on social links (existing pattern).
Migrate ALL colors from hardcoded violet-500/zinc to design tokens. On the yellow contact section, text should be charcoal per the contrast rules.

Export as default: `export default function Footer()`.

**2. Create CompactFooter.tsx for non-homepage pages**

New file. Simple compact footer with:
- `max-w-[1400px] mx-auto px-6 lg:px-12 py-8` container
- Top separator: `border-t border-[var(--color-border)]`
- Flex row (stacks on mobile): left has "Onur Ata Asar" + copyright, right has social icon links
- Social icons: same array as Footer.tsx (GitHub, LinkedIn, Medium) — extract to a shared constant or duplicate (small array, duplication is fine)
- Colors: all design tokens, `text-[var(--color-text-muted)]` for icons, `hover:text-[var(--color-accent)]` on hover
- No framer-motion needed (keep it lightweight as a server component if possible, or "use client" if hover effects desired)

Export as default: `export default function CompactFooter()`.

**3. Update layout.tsx to use CompactFooter**

In layout.tsx, replace the `<Footer />` import and usage with `<CompactFooter />`:
- Change import: `import CompactFooter from "@/components/CompactFooter"`
- The homepage will import and render its own `<Footer />` (the full contact section) directly in page.tsx
  </action>
  <verify>
    <automated>cd /c/Users/onura/Documents/Projects/onurataasar.dev && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Footer.tsx is a full contact section with Section wrapper, split layout, Say Hello CTA, email link, and social links. CompactFooter.tsx is a minimal footer with social icons and copyright. layout.tsx uses CompactFooter. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure homepage with Section color blocks</name>
  <files>src/app/page.tsx</files>
  <action>
Rewrite page.tsx to use the Section component for each content block, creating the poster-gallery layout.

**Structure:**

The homepage renders a sequence of full-viewport Section components. Content inside is PLACEHOLDER for now — Phase 4 (Homepage) will build the real content. This phase focuses on the STRUCTURE and color-blocking.

```tsx
import { Section } from "@/components/Section";
import Footer from "@/components/Footer";
```

Remove "use client" — make it a server component. The FadeIn/Stagger animations will be re-added in Phase 3 (Animations). For now, static rendering is fine.

**Sections in order:**

1. **Hero section** — `<Section id="hero" colorToken="hero">`
   - Keep existing content: greeting, name (oversized with gradient-text), role, brief description, social links
   - Migrate ALL violet/zinc colors to design tokens: `text-violet-500` becomes `text-[var(--color-accent)]`, `text-zinc-600` becomes `text-[var(--color-text-muted)]`, etc.
   - Name should use Syne display font at fluid display size: `font-[family-name:var(--font-display)] text-[var(--font-size-display)] font-bold`
   - Center content vertically within the section using flexbox: the inner content should be wrapped in `flex flex-col justify-center min-h-[inherit]`
   - Keep the socials array and links

2. **About section** — `<Section id="about" colorToken="about">`
   - Move the profile section content here (the "Experienced Frontend Developer..." text)
   - Remove the card/border styling — the section background IS the visual treatment
   - Text color comes from the section text token (cream on charcoal)
   - Add a heading "About" or "Hakkimda" in Syne display font

3. **Skills section** — `<Section id="skills" colorToken="skills">`
   - Keep the skillCategories data array
   - Migrate colors to design tokens
   - Note: pill badges will be redesigned in Phase 4 — keep them for now but with updated colors
   - Text on orange: use charcoal for body text per WCAG compliance

4. **Contact section** — Render `<Footer />` component (the full homepage contact section created in Task 1). This IS the final section with its own Section wrapper built in.

IMPORTANT: Do NOT include a "projects" section yet — that is Phase 4 content. Only include hero, about, skills, and contact.

**Asymmetric layout example (LAY-05):**
- In the hero section, offset the name/text to the left using the 12-col grid: `col-span-12 lg:col-span-8 lg:col-start-1`
- In the about section, offset text to the right: `col-span-12 lg:col-span-8 lg:col-start-4`
- Not every section needs asymmetry — skills can be full-width

**Remove:**
- "use client" directive
- framer-motion imports (FadeIn, StaggerContainer, StaggerItem) — Phase 3 will re-add
- All hardcoded violet/zinc color classes
  </action>
  <verify>
    <automated>cd /c/Users/onura/Documents/Projects/onurataasar.dev && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Homepage renders hero, about, skills, and contact as full-viewport color-block sections. Each section has distinct background color. No hardcoded violet/zinc colors remain. Page builds as server component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete layout system</name>
  <action>Human verifies the complete layout and navigation system across breakpoints and themes.</action>
  <what-built>Complete layout and navigation system: full-viewport color-block sections on homepage, redesigned maximalist navigation with full-screen mobile overlay, split footer treatment (full contact on homepage, compact on other pages), edge-to-edge layout structure.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. **Desktop (1440px viewport):**
       - Verify nav shows "ONUR ATA ASAR" large at left, oversized links at right, theme toggle
       - Scroll through homepage: each section has a distinct bold background color (cream, charcoal, orange, yellow)
       - Each section fills the full viewport height
       - Contact section has "Say Hello" CTA with email and social links
    3. **Mobile (375px viewport or DevTools):**
       - Tap hamburger — full-screen orange overlay should appear with oversized links
       - Social icons and theme toggle visible in overlay
       - Close overlay, scroll homepage — sections should have min-h-[70svh]
    4. **Dark mode:** Toggle theme — section backgrounds should shift to muted variants
    5. **Other pages:** Visit /blog, /notes — should have compact footer (not the full contact section)
    6. **Tablet (768px):** Verify layout works without breaks
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Homepage shows 4 distinct full-viewport color-block sections
3. Footer.tsx renders as full contact section on homepage only
4. CompactFooter.tsx renders on all other pages
5. No hardcoded violet/zinc colors remain in modified files
6. Dark mode transitions smoothly between color variants
</verification>

<success_criteria>
- Scrolling the homepage shows distinct full-viewport color-block sections (cream -> charcoal -> orange -> yellow)
- Contact section has prominent "Say Hello" CTA with email and social links
- Non-homepage pages show compact footer with social icons
- Layout supports asymmetric content placement in sections
- All pages render correctly after layout restructuring
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-03-SUMMARY.md`
</output>
