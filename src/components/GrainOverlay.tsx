"use client";

/**
 * Subtle grain/noise texture overlay (ANIM-05).
 * Hidden when prefers-reduced-motion.
 */
export function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden />;
}
